[keybinding]
# Use Ctrl + b (like tmux) or Ctrl + a (like screen)
prefix = "ctrl-b"

# Map 'd' to detach explicitly
detach = "d"

[session]
# Disable the native prompt modification so Starship can do the work
name_in_prompt = false
