# Extra autostart processes
# exec-once = uwsm-app -- my-service

# Remote desktop via Tailscale (wayvnc)
# Only listens on Tailscale interface with PAM authentication
exec-once = uwsm-app -- wayvnc-tailscale-start
